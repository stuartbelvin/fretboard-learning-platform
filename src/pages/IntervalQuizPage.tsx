import { IntervalQuizTest } from '../components/quiz';

/**
 * IntervalQuizPage - Interval quiz page
 * 
 * Renders the interval quiz component for practicing
 * interval identification on the fretboard.
 */
export function IntervalQuizPage() {
  return (
    <main className="app-main">
      <IntervalQuizTest />
    </main>
  );
}
