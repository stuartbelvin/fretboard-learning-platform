import { NoteQuizTest } from '../components/quiz';

/**
 * NoteQuizPage - Note identification quiz page
 * 
 * Renders the note quiz component for practicing
 * note identification on the fretboard.
 */
export function NoteQuizPage() {
  return (
    <main className="app-main">
      <NoteQuizTest />
    </main>
  );
}
